# Документація проекту SkillHub 🎓

Цей документ є повним звітом по проекту SkillHub, описує його можливості, архітектуру бази даних, роутинг та містить шпаргалку з поясненням ключових частин коду. 

---

## 🚀 1. Огляд проекту (Функціонал)
**SkillHub** — це сучасна платформа для пошуку менторів та планування навчальних сесій. Додаток дозволяє користувачам виступати у двох ролях: **Учень (Learner)** та **Ментор (Mentor)**, а також має систему адміністрування.

### Основний функціонал:
*   **Система Автентифікації:** Надійна реєстрація та логін з хешуванням паролів (Bcrypt) та управлінням сесіями через HTTP-only cookies.
*   **Рольова модель:** Підтримуються ролі `LEARNER`, `MENTOR`, `ADMIN`. Інтерфейс адаптується під поточну роль користувача.
*   **Каталог Менторів:** Динамічний пошук менторів за навичками, категоріями або іменами на головній сторінці.
*   **Бронювання сесій (Booking System):** Інтуїтивно зрозумілий UI для вибору дати та часу (із врахуванням доступності ментора).
*   **Dashboard (Особистий кабінет):** 
    *   **Для Менторів:** Управління статусами сесій (Підтвердження/Скасування), перегляд фінансів (Гаманець), завершення оплачених сесій.
    *   **Для Учнів:** Трекінг своїх уроків, списання кредитів/балансу, доступ до історії.
*   **Система відгуків (Рейтинги):** Після переведення сесії у статус `COMPLETED`, учень має можливість залишити оцінку (1-5 зірок) та коментар. Система захищена від дублікатів (один відгук на одну сесію).
*   **Фінансовий Гаманець (Wallet):** Ментори можуть переглядати свій віртуальний баланс та робити запити на вивід коштів (Withdrawal).
*   **Панель Адміністратора (Admin Panel):** Моніторинг платформи та модерація залишених відгуків (з можливістю їх видалення та автоматичним перерахунком рейтингу).

---

## 🛠 2. Технічний стек (Tech Stack)
Додаток побудований на сучасному стеку технологій екосистеми React:
*   **Фреймворк:** Next.js 14/15 (App Router, Server Actions)
*   **Мова:** TypeScript (сувора типізація даних)
*   **База даних:** PostgreSQL (реляційна БД)
*   **ORM:** Prisma (взаємодія з БД через типізований клієнт)
*   **Стилізація:** Tailwind CSS + Radix UI (shadcn/ui) для доступних компонентів (Accessibility).
*   **Форми та Валідація:** Zod (схеми валідації) + React Hook Form.
*   **Дати та Час:** `date-fns` (точне локальне форматування).
*   **Сповіщення:** `sonner` (Toast notifications).

---

## 📁 3. Архітектура та Структура Проекту

```text
project_kpk/
├── actions/        # ⚙️ Server Actions (Бекенд/Контролери)
│   ├── auth.ts     # Реєстрація, логін, робота з кукі-сесіями
│   ├── feedback.ts # Створення відгуків, перевірка унікальності
│   ├── session.ts  # CRUD операції сесій (Pending -> Confirmed -> Completed)
│   └── user.ts     # Отримання даних ментора та управління гаманцем
├── app/            # 🌐 Роути (Сторінки Next.js App Router)
│   ├── admin/      # Сторінка модерації
│   ├── dashboard/  # Дашборд із вкладками
│   ├── login/      # & register/ (Автентифікація)
│   ├── mentor/[id]/# Динамічний роут профілю ментора
│   └── page.tsx    # Головна сторінка з пошуком
├── components/     # 🧩 Клієнтські та серверні React компоненти
│   ├── ui/         # Базові компоненти (shadcn/ui)
│   ├── booking-modal.tsx  # Клієнтський UI для вибору дати
│   ├── dashboard-view.tsx # Логіка вкладок (з обробкою Hydration Mismatch)
│   ├── review-form.tsx    # Форма оцінювання
│   └── session-card.tsx   # Уніфікована картка уроку з кнопками дій
├── lib/            # 📦 Утиліти та константи
│   ├── db.ts       # Ініціалізація Prisma Client
│   └── utils.ts    # Допоміжні функції (напр. об'єднання класів Tailwind)
└── prisma/         # 🗄️ База даних
    ├── schema.prisma # Опис таблиць і зв'язків
    └── seed.ts       # Скрипт наповнення БД початковими/фейковими даними
```

---

## 🧠 4. Архітектура Бази Даних (Prisma)

*   **User (Користувачі):** Універсальна таблиця для всіх ролей (визначається `enum Role { LEARNER, MENTOR, ADMIN, SUPERADMIN }`). 
    *   *Поля ментора:* `skill`, `category`, `rate`, `rating`, `reviewCount`, `balance`.
*   **Session (Сесії):** Зв'язує Ментора та Учня. 
    *   *Властивості:* `scheduledAt` (дата і час), `status` (PENDING, CONFIRMED, COMPLETED, CANCELLED).
*   **Feedback (Відгуки):** Зв'язані з таблицею `Session` зв'язком **1-до-1** (одна сесія = один відгук). Унікальність гарантує констрейнт `@unique([sessionId])`.
*   **Payment (Платежі):** Транзакції, прив'язані до сесії. Знаходяться у статусах `PENDING`, `COMPLETED` або `FAILED`.
*   **WithdrawalRequest:** Запити на вивід коштів з балансу `User` (Ментора).
