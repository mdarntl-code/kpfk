# 🧑‍🏫 Шпаргалка для Захисту Проекту (Backend & Next.js)

Ця шпаргалка містить відповіді на найпопулярніші запитання, які може поставити викладач під час оцінювання коду.

## 1. "Що таке Server Actions і як вони працюють?"
**Відповідь:** У Next.js 14/15 `Server Actions` — це асинхронні функції, які виконуються суто на сервері. Ми використовуємо їх замість створення старого формату API-ендпоінтів (`/api/route`). 
Коли ми пишемо `"use server"` на початку файлу (наприклад, `actions/session.ts`), ми вказуємо компілятору Next.js зібрати цей код тільки для бекенда. На фронтенді ці функції можна напряму імпортувати у події кнопок (наприклад, `onClick={handleComplete}`) — Next.js самостійно під капотом створить прихований POST-запит до сервера.

## 2. "Як ти запобігаєш дублюванню відгуків?"
**Відповідь:** Це зроблено на двох рівнях:
1. **База даних (Prisma):** У схемі `schema.prisma` поле `sessionId` у таблиці `Feedback` вказано як `@unique`. Prisma фізично не дозволить створити два відгуки для одного уроку.
2. **Бекенд логика (Server Action):** У файлі `actions/feedback.ts` перед створенням відгуку ми робимо запит `prisma.feedback.findUnique`. Якщо відгук вже існує, сервер повертає об'єкт `{ success: false, error: "Ви вже залишили відгук" }`. Це запобігає падінню сервера через помилку БД (`P2002 Unique Constraint Failed`). На фронтенді кнопка ховається, якщо `isReviewed: true`.

## 3. "Як реалізована Автентифікація без сторонніх бібліотек (NextAuth)?"
**Відповідь:**
1. При реєстрації пароль користувача хешується через бібліотеку `bcryptjs` (`hash(password, 10)`).
2. При логіні (`actions/auth.ts`) введений пароль звіряється з хешем у базі через `bcrypt.compare()`.
3. Якщо логін успішний, ID та роль користувача серіалізуються у JSON і зберігаються у захищених `HTTP-Only Cookies` через `cookies().set(...)`.
4. `HTTP-Only` означає, що JavaScript на клієнті не має доступу до кукі, що захищає від XSS-атак (перехоплення сесій).
5. Серверні компоненти (наприклад `page.tsx` в дашборді) здатні читати кукі через `cookies().get()` перед рендером сторінки і розуміти, чи авторизований клієнт.

## 4. "Як відбувається оновлення рейтингу ментора після відгуку?"
**Відповідь:** Оновлення відбувається автоматично у функції `createFeedback` (`actions/feedback.ts`):
Коли учень створює новий відгук, бекенд відразу робить потужний агрегаційний запит до бази даних:
```typescript
const stats = await prisma.feedback.aggregate({
    where: { session: { mentorId: mentorId } },
    _avg: { rating: true },   // Рахує середнє арифметичне ВСІХ відгуків
    _count: { rating: true }  // Рахує загальну кількість відгуків
})
```
Після отримання нових значень (середнє та кількість), ми відразу записуємо їх у профіль ментора (`prisma.user.update`), щоб ці цифри не довелось вираховувати математично під час завантаження списку сотень менторів на головній сторінці. Це значно прискорює загальну швидкодію додатку.

## 5. "Чому деякі компоненти мають директиву `"use client"`, а інші ні?"
**Відповідь:** Next.js за замовчуванням рендерить усі компоненти на сервері (Server Components) для швидкості і покращення SEO (пошуковикам легше читати HTML).
Ми додаємо `"use client"` тільки туди, де необхідна *інтерактивність*:
- Де є кнопки з подіями браузера (`onClick`, `onChange`).
- Де використовуються хуки стану (`useState`, `useEffect`).
- В інтерактивних віджетах (`booking-modal.tsx`, `DashboardView` з вкладками).

## 6. "Що таке Hydration Mismatch і як ти розв'язав цю проблему у Дашборді?"
**Відповідь:** `Hydration Mismatch` — це помилка конфігурації, яка виникає, коли статичний HTML, згенерований на сервері, не збігається з HTML, який намагається відрендерити React завантажившись у браузері. Це часто буває під час рендерингу вкладок (Tabs) або дат, тому що часові пояси (модуль `Date`) на сервері та комп'ютері клієнта можуть бути різні.
*Рішення у файлі `dashboard-view.tsx`:*
Я додав стейт `const [isMounted, setIsMounted] = useState(false)`. Сервер спочатку віддає лише "Скелет" або порожню заглушку (бо спрацьовує умова `if (!isMounted) return <Skeleton />`). Коли сторінка повністю завантажується в браузері, `useEffect` перемикає стейт на `true`, і лише тоді рендеряться складні клієнтські компоненти дат та UI вкладок (Radix UI). Це позбавляє конфліктів.

## 7. "Чому ти використовував `revalidatePath` після Server Actions?"
**Відповідь:** Next.js дужу агресивно кешує сторінки задля швидкодіі. Якщо у серверному екшені ми створили нову сесію або видалили відгук у базі даних, клієнт може не побачити змін на екрані, поки не оновить сторінку двічі. 
Команда `revalidatePath('/dashboard')` наказує серверу Next.js: *"Дані в базі для цього роуту змінилися, очисть внутрішній кеш для цієї сторінки і змусь її відрендерити найсвіжіші дані з бази прямо зараз"*. Це створює відчуття миттєвості для користувача.
